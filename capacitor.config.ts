import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aplikasi Cuaca-ThesKind-IONIC',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
